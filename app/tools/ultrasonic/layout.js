export const metadata = {
  title: "UT Simulator Pro — Ultrasonik Muayene Simülatörü | MetallurgyTools",
  description: "Interactive ultrasonic testing (UT) simulator for NDT training. Simulate A-scan, pulse-echo, angle probe calibration on K1/K2 blocks and flaw detection blocks. Level 3 NDT education tool.",
};

export default function Layout({ children }) {
  return children;
}
