export const metadata = {
  title: "ASTM E45 Inclusion Classification",
  description: "Free ASTM E45 Method A inclusion classifier for steel. Rate Type A/B/C/D inclusions, check acceptance criteria for API 5L, S700MC, DP600, IF steel.",
};

export default function Layout({ children }) {
  return children;
}
