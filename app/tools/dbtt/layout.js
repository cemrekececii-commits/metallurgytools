export const metadata = {
  title: "DBTT Ductile-Brittle Transition Calculator",
  description: "Free DBTT (Ductile-Brittle Transition Temperature) predictor for steel. Estimate Charpy V-notch energy transition temperature and impact behavior based on composition and heat treatment.",
};

export default function Layout({ children }) {
  return children;
}
