export const metadata = {
  title: "API 570 Corrosion Rate Calculator",
  description: "Free API 570 corrosion rate calculator for pressure equipment and pipelines. Calculate remaining wall thickness, inspection intervals, and corrosion allowance for steel equipment.",
};

export default function Layout({ children }) {
  return children;
}
