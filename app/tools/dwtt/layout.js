export const metadata = {
  title: "DWTT Simulation Panel — API 5L Drop-Weight Tear Test | MetallurgyTools",
  description: "API 5L Drop-Weight Tear Test (DWTT) simulator. Configure impact energy for X42–X80 pipeline steel grades. Calculates required Joules based on yield strength, wall thickness, and machine drop height. 55,000 J Pragya machine interface.",
};

export default function Layout({ children }) {
  return children;
}
