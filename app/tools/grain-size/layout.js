export const metadata = {
  title: "ASTM E112 Grain Size Analyzer",
  description: "Free ASTM E112 grain size measurement tool for steel microstructure analysis. Convert between grain number, equivalent diameter, and grain boundaries per inch.",
};

export default function Layout({ children }) {
  return children;
}
