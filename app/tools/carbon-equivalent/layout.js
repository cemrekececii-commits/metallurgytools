export const metadata = {
  title: "Carbon Equivalent Calculator | Weldability",
  description: "Free carbon equivalent calculator for steel weldability assessment. Calculate Ceq and Pcm to predict cold cracking risk and determine required preheat temperature.",
};

export default function Layout({ children }) {
  return children;
}
